[pytest]
testpaths = tests
addopts = --browser chromium --tracing retain-on-failure --screenshot only-on-failure --output output_ui_checks/playwright
markers =
    e2e: browser end-to-end checks
    network: scenarios that require multiplayer room flow
    slow: longer-running scenarios
